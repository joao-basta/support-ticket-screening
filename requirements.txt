# Caixa Log Diagnostic Pipeline (LDP) - dependências de execução
#
# A instalação canônica é `pip install -e .` (ver pyproject.toml). Este arquivo
# existe para ambientes que exigem requirements.txt e deve espelhar o pyproject.
#
# Ambiente alvo: Python 3.10+ (validado em 3.12). O pipeline roda 100% offline:
# nenhuma dependência abaixo faz chamada de rede em tempo de execução.

# --- Motor NLP (Passo 1) ---
scikit-learn>=1.3.0
numpy>=1.24.0

# --- API HTTP (Fase 7) ---
# Flask 3.x é obrigatório: a série 2.3 lê `werkzeug.__version__`, atributo que o
# Werkzeug 3 removeu, e o par antigo quebra ao criar um test client.
Flask>=3.0
Werkzeug>=3.0

# Removidos nesta fase: `nltk` e `spacy` eram declarados mas não importados em
# nenhum módulo — instalação pesada, sem uso. O restante do pipeline
# (anonimização, parsing GLPI, análise de log) usa apenas a Standard Library.
